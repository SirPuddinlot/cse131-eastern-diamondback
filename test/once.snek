(let ((x 0) (sum 0) (n 0))
  (block
    (set! n input)
    (loop
      (if (< x n)
        (block
          (set! sum (+ sum (* x 2)))
          (set! x (add1 x))
        )
        (break sum)
      )
    )
  )
) 